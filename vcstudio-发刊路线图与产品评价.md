# VASP Catalyst Studio — 发刊路线图 · 产品评价 · 改进建议

日期：2026-07-16 ｜ 基于：代码库体检 + 两组文献调研 + OpenClaw 论文(JCTC 2026)对标 + 两篇吸附能学位论文 + 你的产品愿景

---

## 零、一句话结论

你的软件**架构已经踩在正确的方向上**（确定性核心 + 有界恢复 + 桌面 GUI + 催化专注），和刚发在 JCTC 的 OpenClaw 几乎是"同一套骨架的两种哲学"。距离发刊，缺的不是更多功能堆砌，而是三件事：**(1) 一个能复现已发表结果的催化案例；(2) 不依赖 Origin/POV-Ray 的原生论文级出图与"按组"的结果分析；(3) 面向新手的极简可视化 + 工程化打磨**。下面逐条展开。

---

## 一、发刊定位：你 vs OpenClaw(JCTC 2026)

OpenClaw 那篇（Ding, Zeng 等，ECNU+USTC，J. Chem. Theory Comput. 2026, 22, 5919）的骨架与你惊人平行：

| 组件 | OpenClaw | 你的 vcstudio |
|---|---|---|
| 任务清单/规划 | task manifest skill | job.yaml 状态机 |
| 集群执行接地 | DPDispatcher skill(PBS/Slurm/LSF) | cluster/ 的 PBS·Slurm 方言 |
| 失败恢复 | bounded recovery + 人工兜底 | 有界恢复(≤3轮)+ NEEDS_HUMAN |
| 决策 | **LLM 全程在环**(GPT-5.5) | **确定性核心零 token**，LLM 仅分析层 |

关键：**OpenClaw 论文自己承认的软肋，正是你的卖点**——它写明"LLM 随机性导致 run-to-run 结果从完美到 silent failure 不等"、"单次运行约 $7.6 token 成本"、"物理正确性仍需专家把关"。你的确定性核心从架构上消解了这些审稿人第一质疑点：**结果可复现是"保证"而非"统计"、零 token、幻觉被隔离在不影响正确性的层**。

**差异化一句话**：在"VASPKIT 只做后处理 / atomate·AiiDA 需编程 / LLM-agent 不可复现"三者的空白里，你是**桌面端、点击式、确定性、催化专用**的那一个。这是干净、好讲、审稿人爱听的故事。

**现实可投去处**：JOSS 或 SoftwareX（软件类，需 6 个月开发史 + 文档 + 测试 + code metadata）；Computer Physics Communications（VASPKIT/DP-GEN 同系，认可度高）；若补出催化科学发现，可冲 Digital Discovery / npj Comput. Mater.。

**发刊必须补齐的三张表 + 一个案例**（审稿硬通货）：
1. 与 pymatgen **custodian** 的错误处理逐条覆盖对照表（custodian 是"错误恢复覆盖"的事实标准，约 20 类 VASP 报错）。
2. 与 VASPKIT / atomate2 / AiiDA 的功能矩阵对比表。
3. SoftwareX 式 code metadata（版本、许可、依赖、仓库、DOI）。
4. **一个复现案例**：用你给的张洪毅论文体系（SAC/石墨烯 M-Nx 吸附 LiPS），vcstudio 自动跑通 → 吸附能/ΔG 与论文表 3-2 对齐、报误差。**这是最有杀伤力的一张图。**

---

## 二、以"用 VASP 算吸附能的学生"视角，从头到尾点评

### 便利性（你的核心诉求：点击即可、不记命令）
现状：Web GUI 四页（生成/项目/任务/集群）已经把"生成四件套 + SSH 提交 + 状态查询 + 续算"变成点击操作，方向完全对。**但对纯新手仍有门槛**：
- 生成页要用户**自己准备好 POSCAR 和 INCAR**。新手最卡的恰恰是"怎么建 slab、怎么摆吸附质、INCAR 该写啥"。**建议**：加"参数向导"——选体系类型/泛函/精度档位（粗测/标准/高精），软件按催化惯例（PBE+D3、ENCUT 500-550、真空 20Å、含 TM 开自旋、含氧化物提示 +U）自动产出一份可编辑的 INCAR 草稿；再配吸附位点枚举 + 分子库自动摆放，新手就不用碰结构文件了。
- 集群设置、赝势库路径这些一次性配置，建议做成首启"设置向导"，并把出错信息中文化、可点击修复。

### 任务栏/结果栏混乱（你已点名）——**按"组"归并**
现状：任务是**一个作业一行**平铺，算一个吸附能其实牵扯 clean_slab + gas_ref + 多个吸附组态，散在列表里看不出归属。
**建议（高优先，直接改善体验）**：引入"**吸附能项目组**"为一等对象——一组 = 清洁基底 + 参考物 + N 个吸附构型。任务页按组折叠：组头显示"整组进度 3/5 完成、ΔE 待全 DONE"，展开看成员。你已有 `project/` 吸附能项目的骨架（clean_slab/gas_ref/configs），把它提升成任务页的默认视图即可。ΔE 门控（全 DONE 才出数）已经在，只是没在 UI 上"成组"呈现。

### 结果分析（你觉得要加）——**这是与发刊差距最大的一块**
两篇论文的固定论证链是 **吸附能(锚定) → ΔG 台阶(决速步/极限电位) → 电子结构成因(PDOS/d带中心/COHP/Bader/差分电荷)**。你现在只做到第一段的一半（ΔE 数值），**分析深度和出图质量都要补**（见第三节）。

### 验证机制（你点名）
现状亮点：诊断器 + 有界恢复 + ΔE 门控 + 能量合理性闸（正能量不入表）已经是很好的"结果可信"护栏。**发刊级还需补**：
- **数值对标协议**：内置"跑一个已知体系 → 与文献/实验值比 MAE"的验证流程（如复现论文 Ead 表），并写进 docs/validation.md。
- **可复现性证据**：同输入多次跑 bit-level 一致（你确定性核心天然能给，竞品给不出）——这点要在论文里显式展示。
- 输入科学性护栏（我已在做/建议做）：项目内 ENCUT 强制统一（防 ΔE 大数相减被不同基组污染）、KPOINTS 倒格矢（已修）、含氧化物提示 +U、含 TM 提示自旋、真空层检查。

---

## 三、原生论文级出图（脱离 Origin/POV-Ray）

**核心判断：完全可行，且应该做。** 用 matplotlib（你已依赖）就能复刻论文图，不必要求用户装 Origin；结构图用 matplotlib 3D 或 ASE 渲染替代 POV-Ray。把 Origin/POV-Ray 降级为"可选增强"，**默认走原生引擎**，保证任何用户开箱即得论文级图。

按优先级（对标论文图号）：

1. **吸附能分组柱状图 + 数据矩阵表**（对标论文2 图3.23a、论文1 表3-2）：一组吸附质 × 一组基底的 ΔE 分组柱，柱顶标数值，纵轴按锂硫惯例反向；配 CSV + 三线表。**你现在就有 ΔE，能立刻产出。**
2. **ΔG 自由能台阶图**（对标图3.10/4.3/3.23c）：多体系叠加折线台阶，标决速步、极限电位 U_L。**前置依赖：ZPE/熵修正**——需补频率计算的生成端（现在 thermo.py 只有解析端），这是"从纯吸附能升级到论文级 ΔG"的关键缺口。
3. **吸附构型俯视+侧视双面板 + 自动键长标注**（对标图3.6-3.9）：后处理 CONTCAR，matplotlib 出图，元素色球图例（VESTA 配色）。
4. **PDOS + d 带中心自动标注**（对标图3.15/4.5）：扩 dosparse 解析 partial 投影段（现在只读 total），自旋镜像、E_f 竖线、d 带中心积分标注。
5. **差分电荷密度 / COHP-ICOHP / Bader**（进阶，依赖 VASPKIT/LOBSTER 解析或自算）。
6. **火山图 / NEB 能垒图**（支撑"最优催化剂筛选/充电脱锂"叙事）。

视觉规范（供原生引擎统一）：多面板 a/b/c/d 标号、serif 字体、刻度朝内、撞色柱、10 色定性循环台阶、费米能级虚线、等值面黄绿/青蓝。这些我会固化成一个绘图风格模块。

---

## 四、一键报告 + 设置项（API/提示词自由度，你点名）

- **一键报告**：选中一个"吸附能项目组"→ 自动汇总 ΔE/ΔG 表 + 上述图 + （可选）LLM 文字解读 → 导出 Word/PDF/HTML。你已有 report/report_full 骨架 + tools/generate_word_report.py，把它接到"组"上、并把原生图塞进去即可。
- **设置页（给用户自由度）**：
  - 自选 LLM API：base_url + 模型名 + key（key 走 keyring，不落明文）——你 config 已有 llm 小节，补一个设置 UI 即可。
  - **提示词预设 + 自定义**：内置几套预设（如"严谨方法学复述"/"机理解读"/"审稿视角自查"），用户可选可改可存。确定性核心零 token 不变，LLM 只在用户主动要分析时按需调用。
  - 精度档位、默认泛函/色散/自旋/+U 开关等生成默认值，也放设置页。

---

## 五、建议的推进优先级（把你的诉求排进原有四批）

已完成并入库：批1 加固（config 脱敏/版本/依赖/CI）、批2 部分（**续算状态误识别修复** ✅、calc_type ✅、KPOINTS 倒格矢 ✅）。

接下来建议顺序：
1. **批2 收尾**：项目内 ENCUT 强制统一 + 诊断补 IO/磁盘满/137 签名（对标 custodian 覆盖，直接进发刊的错误表）。
2. **任务按组归并 + dashboard 首页**（体验，你点名的"混乱"）。
3. **原生出图引擎**：先做①吸附能柱状图+表、③构型双视图（不依赖新计算），再做④PDOS/d带（扩 dosparse）。
4. **设置页**：LLM API 自选 + 提示词预设 + 生成默认值。
5. **一键报告**：接"组" + 原生图 + 可选 LLM。
6. **ΔG 升级**：频率生成端 → ②自由能台阶图（打通论文级 ΔG）。
7. **发刊三张表 + 复现案例**：custodian 对照、功能矩阵、code metadata、张洪毅体系复现。
8. 批3 性能（SSH 合并+并发/OUTCAR 流式）、批4 重构（穿插进行，降双写）。

---

## 六、给你的几个待决问题（我们讨论后再动手大功能）
1. **出图**：是否确认"默认原生 matplotlib，Origin/POV-Ray 降为可选"？（我倾向是）
2. **复现案例**：先拿张洪毅论文的哪个体系做首个对标——SAC/石墨烯 M-Nx（案例A，最全）还是 CoO/Co9S8 表面（案例C，含 +U）？
3. **ΔG 升级**：是否要我把频率计算的生成端补上（这是论文级自由能台阶的前提，工作量中等）？
4. **优先级**：上面 8 步里，你最想先看到哪 2-3 个落地？
