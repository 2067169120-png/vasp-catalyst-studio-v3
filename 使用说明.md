# VASP Catalyst Studio 使用说明

轻量化 VASP 自动化桌面平台(Windows 双击 EXE)。定位:**吸附能研究全流程**——生成输入 → 提交集群 → 监控/失败诊断 → 有界恢复 → ΔE/自由能分析 → 出版级报告。架构对齐论文(OpenClaw, *JCTC* 2026):确定性核心零 token、显式验证条件、有界恢复、外部软件适配器化。

---

## 一、代码分区块地图(每部分代码的作用)

```
vcstudio/
├─ generate/   【输入生成区】纯函数,离线可测
│   ├─ poscar.py          POSCAR 解析(物种/计数/晶格)
│   ├─ incar_builder.py   INCAR 解析+校验补全(缺 ENCUT/MAGMOM 才补,绝不改你已写的键)
│   ├─ potcar.py          POTCAR 拼接(62 元素 ENMAX 表,库缺失显式报错)
│   ├─ kpoints.py         KPOINTS 推荐(molecule=Γ点 / slab / bulk 按晶胞)
│   └─ job_builder.py     编排:POSCAR+INCAR → 四件套目录(生成页背后就是它)
│
├─ cluster/    【集群区】提交/监控/诊断/恢复;SSH client 可注入离线测
│   ├─ schedulers.py      PBS+Slurm 方言(纯函数:指令/作业号/状态/终态原因解析)
│   ├─ script_builder.py  提交脚本双轨(auto=资源参数生成;template=你的模板逐字透传只填占位符)
│   ├─ connection.py      SSH 连接(跳板机/known_hosts 安全门控;paramiko 只在此处)
│   ├─ submitter.py       提交→查状态→取证→结果拉回→CONTCAR 续算(状态门+3 轮上限)
│   ├─ diagnose.py        失败分类器:12 类(SIGSEGV/OOM/墙钟/未收敛…)+ 11 类 VASP 内部
│   │                     错误签名(Custodian 核对)→ DONE/UNCONVERGED/FAILED/NEEDS_HUMAN
│   ├─ profiles.py        集群档案(clusters.yaml;密码只进 Windows 凭据库)
│   └─ ledger.py          本地台账(只记路径;状态真相永远现读各目录 job.yaml)
│
├─ project/    【分析报告区】
│   ├─ adsorption.py      吸附能项目:批量生成/ΔE 门控(成员全 DONE 才给数)/CSV 导出
│   ├─ freeenergy.py      Li-S 放电路径:ΔG/决速步 PDS/极限电位 U_L;μ_Li 从分子库估算
│   ├─ charts.py          四种图 SVG 引擎(兜底层;Paul Tol 出版级配色)
│   ├─ ai_analysis.py     LLM 分析:真实 INCAR 注入/禁编造文献/双语输出;key 在凭据库
│   ├─ report.py          批量状态报告(HTML/Markdown,零依赖)
│   └─ report_full.py     完整报告组装:ΔE 表+Origin 图+结构图廊+自由能+AI 章节
│
├─ external/   【外部软件适配器区】运行时探测→调用→失败降级,EXE 不打包重依赖
│   ├─ origin_charts.py   Origin 出版级出图(外呼系统 Python 跑 originpro;PNG+.opju 存档)
│   └─ povray_render.py   POV-Ray 分子球棍图(自解析 POSCAR→.pov→pvengine64,顶/侧视)
│
├─ gui_web/    【界面区·默认】pywebview 前端 + api.py 薄门面;**五页**:
│   仪表盘 / 生成 / 吸附能项目 / 任务 / 集群(assets/*.js,零后端框架)
├─ gui/        【界面区·旧版】四页 Tkinter(`vcs gui --legacy` 回退);后台线程不卡界面
│
├─ shared/     【共享区】manifest.py=job.yaml 状态机(9 态+溯源审计)/config.py/secrets.py
└─ cli/        【命令行】vcs gen / vcs gui 等子命令
tests/         纯函数离线测(格式变体夹具 + ASE 对拍;POV-Ray/Origin 真机冒烟)
docs/superpowers/specs/   设计文档(每轮大改动一份 spec)
```

**分层规则**:generate/cluster/project 绝不依赖 GUI;外部软件只经 external/ 适配器;
job.yaml 是唯一状态真相;LLM 只出现在 ai_analysis.py(分析层),核心流程零 token。

---

## 二、快速开始(新用户从零)

首次按下面顺序走一遍;之后各页会记住你的输入。**界面默认 Web**(pywebview),
命令行 `vcs gui` 启动;想用旧版 tkinter 加 `--legacy`。界面共**五页**:
**仪表盘 / 生成 / 吸附能项目 / 任务 / 集群**。

0. **启动**:双击 `dist\VASP Catalyst Studio.exe`(或命令行 `vcs gui`)。

1. **配赝势库(必需一次)**:生成页填「POTCAR 库根目录」,指向你机构授权的 PAW
   赝势库(如 `.../potpaw_PBE`)。赝势是版权材料,本仓库不分发;想先离线体验,用
   `examples/quickstart` 的**假赝势库**生成脚本。

2. **配集群(要提交才需要)**:集群页新建集群档案(主机/端口/用户名/认证方式/
   跳板机/远程根目录/调度器 PBS 或 Slurm)→「测试连接」(首次输密码,存进 Windows
   凭据库)→ 预览提交脚本确认无误。

3. **生成输入**:
   - 单作业:生成页选 POSCAR + 你的 INCAR + 计算类型(slab/bulk/molecule)+ 输出目录
     →「生成」,出 INCAR/KPOINTS/POSCAR/POTCAR 四件套 + `job.yaml`。
   - 吸附能项目:吸附能项目页填 项目名 / 输出根目录 / 清洁表面 POSCAR / 共享 INCAR /
     多个吸附组态(可选气相参考)→「批量生成」(项目内 ENCUT 自动统一,保 ΔE 基组一致)。

4. **提交 + 监控**:任务页选中作业(或整个吸附能项目组)→「提交」→ 勾「自动刷新」。
   状态实时回读各作业 `job.yaml`;失败作业「诊断」列给失败分类 + 证据 + 是否可续算(♻),
   分类学见 [docs/failure-taxonomy.md](docs/failure-taxonomy.md)。

5. **有界恢复**:标 ♻ 的选中点「续算」(CONTCAR 续,INCAR 冻结,**上限 3 轮**);
   NEEDS_HUMAN 的看证据人工处理。

6. **分析 / 出图 / 报告**(全部 DONE 后,回吸附能项目页**手动触发**):
   - 「算 ΔE」:成员全 DONE 才给数,缺谁明说;可导出 CSV。
   - 「论文级出图」:勾图类型一键出图(单项目:柱状图/数据表/ΔG 台阶;多项目对比:
     热图/标度关系/火山图)——**原生 matplotlib 引擎**(PNG+PDF),不依赖 Origin。
   - 「生成完整报告」:一份自包含 HTML(状态总览 + ΔE 表 + 图 + 结构图廊 + 方法学约定 +
     可选 AI 分析)。Origin/POV-Ray/LLM 都是**可选外部件**,缺了自动降级(改内嵌 SVG、
     AI 章节标注"未运行"),报告照出。

> 想不装 VASP/集群先验证分析链路?跑 `python examples/offline_analysis/run_demo.py`
>(一组合成的"已完成"作业 → 诊断 → ΔE → 出图 → 报告,全离线)。

## 三、报告内容

- **状态总览**:计数卡片 + 问题作业清单(失败分类+证据+可否续算)
- **ΔE 表**:成员全 DONE 才给数,缺谁明说;有续算历史的行注"续算×N"
- **计算参数 + 赝势身份**:INCAR 关键键 + KPOINTS 网格 + POTCAR 身份小表
  (element/variant/TITEL/ENMAX),发刊级可复现
- **图表**:「论文级出图」默认原生 matplotlib(PNG+PDF);完整报告里的图**优先 Origin**
  (若装了 originpro,工程存 `report_figs/*.opju`),否则自动换**内嵌 SVG**——两条路都出得来
- **Li-S 放电路径**:ΔG 阶梯图,PDS 红色高亮,U_L 标注(U_L 相对 S8/Li₂S 整反应平衡电位,非 Li/Li⁺)
- **结构图**:POV-Ray 球棍图顶/侧视(可选,缺则跳过)
- **AI 分析**:中文解读+英文论文段落+caveats+置信度(可选,无 key 时给离线提示词包)

## 四、科学约定(论文方法学,代码同款)

- **E_ads = E(slab+ads) − E(slab) − E(ref)**,负 = 有利吸附
- **μ_Li = (E(Li₂S) − E(S₈)/8) / 2**(可显式覆盖);ΔG 参照 S8* = 0
- 所有能量为 **DFT 电子能(E0),未含 ZPE/熵**——报告与 AI 提示词中均明示
- 平台**绝不自动改你的 INCAR**(方法学主权);诊断只给建议
- 有界恢复:续算上限 3 轮;规则不覆盖 → NEEDS_HUMAN 停机交人,绝不静默
- 密码/API key 只进 Windows 凭据库,绝不落明文文件

## 五、常见问题

| 现象 | 处理 |
|---|---|
| 界面起不来/白屏 | 确认装了 pywebview(`pip install pywebview`)+ Windows Edge WebView2 运行时;或用 `vcs gui --legacy` 回退旧界面 |
| 自动刷新提示"需先手动查询一次" | 手动查一次状态输入密码(存入凭据库),之后自动 |
| 完整报告里的图是 SVG 不是 Origin | **正常降级,不是错误**:Origin 是可选外部件;「论文级出图」本就走 matplotlib,完整报告在没装 originpro 时自动换内嵌 SVG |
| 结构图跳过 | POV-Ray 为可选;确认 POV-Ray 在默认安装位,或 config 给 pvengine64 路径 |
| AI 分析未运行 | AI 为可选层;确认凭据库有 key,或用报告中的提示词包走离线路线 |
| bat 乱码/不执行 | bat 必须 GBK 编码+CRLF,别存成 UTF-8 |
| CLI 用法 | 生成:`vcs gen --poscar POSCAR --incar my.incar --calc-type slab -o out/`;界面:`vcs gui`(`--legacy` 用旧 tkinter) |

## 六、开发者备注(本机现成配置备忘)

> 以下是**开发者本机**已配好的环境备忘;**新用户按上面「快速开始」自行配置即可,
> 不必照抄这些路径**。

- 本机已就绪:POTCAR 库、Li-S 分子库(`E:\V2.0.0\results\done\lis_results`)、
  理想窗口带(-2.90~-1.65 eV)、DeepSeek key(Windows 凭据库)、Origin2024b、POV-Ray 3.7。
- 改完代码双击 `重新打包EXE.bat` 重打包;库开发用 `pip install -e .[dev]`,
  跑测试 `python -m pytest`。
- 离线体验假赝势库:`examples/quickstart`;离线分析链路演示:`examples/offline_analysis`。
