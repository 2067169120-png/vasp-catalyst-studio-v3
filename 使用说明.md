# VASP Catalyst Studio 4.0 使用说明

VASP Catalyst Studio 4.0 是一个以项目为中心、证据驱动、可配置的材料与催化研究工作台。默认 Web 界面把结构准备、输入生成、提交监控、失败诊断、分析、出图和报告放在同一项目上下文中。

它不是“自动替你判断科学结论”的黑箱。所有自动化都受状态、证据、方法一致性和预算门禁约束；证据不足时保留 `unknown`、阻止继续或交给人工。

> 英文指南：[docs/user-guide-en.md](docs/user-guide-en.md)
>
> 报告合同：[docs/report-state-contract.md](docs/report-state-contract.md)
>
> 失败分类：[docs/failure-taxonomy.md](docs/failure-taxonomy.md)
>
> 验证边界：[docs/validation.md](docs/validation.md)

## 一、安装与启动

### 1. Windows 打包版

双击：

```text
dist\VASP Catalyst Studio.exe
```

默认打开 Web 工作台。若启动失败，先确认 Microsoft Edge WebView2 Evergreen Runtime 可用；启动错误日志写入用户配置目录中的 `vcstudio/startup-error.log`。

### 2. 从源码运行

```powershell
pip install -e ".[dev]"
vcs gui
```

也可以直接运行 Web 模块：

```powershell
python -m vcstudio.gui_web
```

入口不要混淆：

| 命令 | 实际界面 |
|---|---|
| `vcs gui` | 当前默认 Web 工作台 |
| `python -m vcstudio.gui_web` | 当前默认 Web 工作台 |
| `vcs gui --legacy` | legacy Tkinter 四页 |
| `vcs-gui` | legacy Tkinter 四页；不是默认 Web |

旧 Tk 界面只用于兼容，不代表当前 Web 工作台的信息架构，也不提供 Project lifecycle、Selection Tray、Resume Center、report insights、实验室策略或治理后的下一步建议。

## 二、当前界面结构

默认 Web 工作台有 **7 个一级区域、32 个语义路由**。底层复用 **11 个普通物理页面**；AI Assistant 以 drawer 形式打开，不另算普通页面。

| 一级区域 | 常用路由 |
|---|---|
| Home 首页 | 全局概览、项目管线、最近活动 |
| Project 项目 | 项目概览、成员、工作流、运行记录、项目活动 |
| Prepare 准备 | 结构、输入、批量准备、TaskCatalog、preflight |
| Run 运行 | 作业、远程集群 |
| Analyze 分析 | 吸附、热力学、电子结构、电荷、比较、自定义、性质 |
| Publish 发布 | 图表、报告、SI、草稿包、版本历史、导出 |
| Environment 环境 | 集群、本机运行器、依赖、数据路径、模板、设置 |

选择项目后，全局项目上下文会传递给运行、分析和发布工具。深链接指向已移动或未登记项目时，界面会明确提示，不会猜测替代项目。离开含未保存内容的页面前会给出清单和确认。

Home 的 **Resume Center** 列出本次会话可继续的设置、导入流程和报告配置草稿。它只保存有界草稿引用；点击“继续”后仍由对应组件重新读取服务端权威状态。浏览器草稿不会因此变成项目事实、作业状态或已发布 revision。显式保存的设置和已发布报告本来就是持久的，不依赖 Resume Center。

Home 和 Project 都提供 **Research Explorer（跨项目研究浏览器）**。它不会增加新的一级区域或
语义路由，而是在两个入口显示同一份服务端状态：

1. 用元素、化学式、facet、adsorbate、任务、状态、方法指纹、证据等级、能量/能垒范围筛选；
2. 默认保持“方法兼容”勾选。系统只保留一个方法 cohort；不要为了得到更多点而关闭它；
3. 表格、histogram、scatter 和元素周期表中的数值、单位、样本数、缺失数都由后端计算，页面
   不会重新计算科学值；
4. 点击表格或散点的“溯源”可查看 live data/logical provenance。这里显示的是当前本地状态，
   不是 Publish → Versions 的 frozen Evidence Graph；
5. “保存当前 filters / sort / axes”只保存视图配置。发生 revision conflict 时应采用服务端返回的
   新 authority 状态后重新确认，不会自动覆盖另一会话；
6. freshness 为 partial、stale 或 unavailable 时，页面会扣留科学结果。先点“重建索引”并处理
   不可读项目/manifest；不要把可读子集当成完整分母。

索引本身是内存中的可重建读模型，不写数据库、不修改 `project.yaml`/`job.yaml`，也不授权提交、
续算、报告 accepted 或 publication gate。

## 三、首次配置

### 1. 赝势与数据路径

在 Environment → Settings 配置：

- VASP POTCAR/PAW 库根目录；
- 分子库；
- 输出根目录；
- 需要时的参考数据路径。

POTCAR 是受许可证约束的材料，本仓库和发行包不分发。想先体验输入与分析流程，可使用 [examples/quickstart](examples/quickstart/README.md) 提供的假赝势库；它不能用于真实计算。

### 2. 集群

在 Environment → Cluster：

1. 新建主机、端口、用户名和远端根目录；
2. 选择 Slurm 或 PBS；
3. 按需配置 ProxyJump；
4. 测试连接并逐字核对主机名、算法和 SHA256 指纹；
5. 预览提交脚本；
6. 密码只保存到系统凭据库。

未知主机缺少可核验指纹时连接会被阻止。作业、远端目录和服务器归属有硬门禁，不能跨服务器续算、取消或下载。

### 3. 工作模式与引擎

在 Settings 选择工作模式、计算引擎和本次计算类型。这个选择只裁剪可见路由和可用任务，不会改写已存在作业的事实。

VASP 是主引擎。CP2K、Gaussian、CASTEP 只显示当前后端能力清单明确支持的任务。

Environment → Settings 还提供**实验室建议策略**。模板可以给出方法与资源建议，并允许在白名单字段内显式覆盖；预览不会改动任何作业。只有用户确认后才保存带 revision/hash 的策略选择，而且它仍是 `recommendation_only`：不会回写既有 `job.yaml`、修改已生成输入或提交作业。其他会话先更新策略时，当前确认会因 revision 冲突而要求重新检查。

## 四、推荐工作流

### 1. 建立、复制、移动或认领项目

在 Project 中建立/导入项目，检查项目成员、参考态、方法指纹和工作流阶段。一个项目可以关联多个作业，但项目状态不能从页面显示文字反推，必须读取实际 manifest、证据和门禁结果。

Project lifecycle 的身份规则如下：

| 操作 | 身份与路径语义 |
|---|---|
| Clone | 从当前项目复制到新目录，并铸造新的项目 UUID/opaque ID 与远端 namespace；只重写项目内部 locator，外部参考仍保持外部参考 |
| Move | 移动当前项目并保留项目 UUID/opaque ID；项目内部 locator 重定位，注册表以同一身份原子替换旧位置 |
| Adopt Copy | 认领一个已复制的项目目录；默认选择 **Remint identity**，避免两个目录共享同一身份。只有用户显式选择 preserve 且没有身份/路径冲突时才保留身份 |

三种操作都先由服务端选择源/目标并执行 dry-run preflight；在用户点击确认前不会复制、移动或写注册表。确认时会重新检查项目、注册表和台账是否自预检后发生变化，目标已存在时绝不覆盖。注册表或台账写入失败会回滚文件、`project.yaml` 和登记记录；如果底层文件系统阻止完整回滚，界面显示 partial rollback/恢复证据并停止，而不是报告成功。Clone/Move/Adopt 的进度也会进入统一 Operation Queue。

Project 页面还提供 **Research Notebook + Human Review Ledger**：

- Note 可标记为问题、假设、观察、解释、限制或下一步；Decision 与 Human Review 独立记录；
- 每次保存都在 revision+head+project CAS 下追加新的 journal revision；项目外本机 head/sequence anchor 会把缺失、截断或回滚的 journal 标为 tampered。修订通过 `supersedes` 关联旧记录，删除通过 tombstone 保留历史；
- 可关联 opaque project/job/source/report revision ID。读取时服务端重新核对证据，显示 current、stale 或 missing，并用语义路由跳转；缺失或损坏的 `job.yaml` 一律是 missing，不会由目录推断成 current；
- 附件通过本机选择器进入项目内 content-addressed 存储；浏览器 DTO 只有名称、大小、媒体类型和 hash。notebook/attachment/anchor/lock 路径拒绝 symlink、junction 与 reparse point，读取和归档都会复验 blob hash；
- Reviewer ID、姓名、角色、decision、requested changes 和 signature attribution 都由真人在本机明确录入。它们是自声明归属，不是认证或密码学电子签名；
- AI proposal 不能写成 Human Review。任何 notebook review 都不会自动改变 `ValidationResult`、claims、final、campaign accepted 或 `human_scientific_reviewed`。

未保存正文与附件字节不会进入 workspace localStorage。Resume Center 只保留带 opaque project ID 的安全 marker，因此可以回到对应项目，但不能恢复尚未保存的正文。

### 2. 准备结构与输入

Prepare 提供：

- 结构加载、编辑、固定底层和真空检查；
- 分子库、金属 slab、SAC 候选矩阵和多自旋家族；
- VASP 23 类 TaskCatalog；
- NEB、批量输入和 preflight；
- POSCAR、INCAR、KPOINTS、POTCAR 四件套生成。

平台只补齐缺少的 INCAR 键，不覆盖用户已经给出的键。生成结果会写入 `job.yaml`、输入 hash 和来源记录。

CLI 也可生成最小 VASP 作业：

```powershell
vcs gen --poscar POSCAR --incar my.incar --calc-type slab -o results/job1
```

### 3. 提交、批量复核与监控

Run → Jobs 支持：

- 单个或批量提交；
- 多服务器状态刷新；
- 调度器状态、输出完整性和收敛证据查询；
- 关键结果下载；
- 远端文件浏览；
- 本机运行器；
- 派生静态、频率、AIMD 等后续作业。

`job.yaml` 的九态生命周期记录运行过程。当前目录、服务器和输入不满足门禁时，提交在联网前阻止。

勾选多个作业后，**Selection Tray** 会列出完整选择；被当前状态、集群或项目筛选隐藏的作业也会计入并明确标出。展开 **Batch Review** 后再执行 Submit、Fetch、Continue、Cancel 或 Remove。页面在确认开始前取得互斥操作锁，避免双击启动第二个批操作；提交、续算和取消还使用服务端幂等键，同一载荷的重试只重放既有结果，正在执行的重复请求保持 busy，不会再次产生远程副作用。Jobs、Project lifecycle、Analysis 与 Publish 的长操作统一进入全局 **Operation Queue**，显示 confirming/running/succeeded/failed，而不是各页各自伪造完成状态。

点击 **检查重复计算**，或直接开始提交，服务端会从当前 ledger、权威 `job.yaml` 和文件
hash 重建容量有界的严格指纹索引。面板显示指纹字段、exact match、near match 的逐字段
差异、失败/未收敛/不完整来源、节省核时估算和来源重验状态。near match 绝不代表等价；
缺 Recipe、VASP/build 环境或任何必需证据的旧作业显示 `incomplete / explicit_legacy`。
默认仍会重新计算。若选择 **引用既有结果**，后端会再次重验来源并为新作业写独立
provenance node/link 和 reuse decision；不会继承 accepted/final。若已有可重验 exact match
但仍要重算，必须填写理由，理由与当前指纹一起持久化。浏览器只传 opaque job ID，不读取
或计算 POSCAR/INCAR/KPOINTS/POTCAR 指纹。

Selection Tray 的**只读资源预测**从服务器台账、manifest 与相同任务类型的历史记录估算 core-hours 点值、区间、置信度、失败率和预算风险。批量预算使用区间高端评估；历史稀疏或输入缺失时显示低置信度/unavailable，不猜数值。预测始终 `recommendation_only`、`authorizes_submission=false`，不会取代提交确认、主机信任或幂等门禁。

### 4. 诊断与有界恢复

当前分类器有 **18 个作业分类结果**和 **16 条 VASP 内部错误签名**，最终归入：

| 状态 | 含义 | 系统动作 |
|---|---|---|
| DONE | 收敛且要求的结果完整 | 可进入后续验证 |
| UNCONVERGED | 运行结束但未满足收敛条件 | 仅在可恢复类别下提供续算 |
| FAILED | 有明确失败证据 | 展示证据和建议，不自动改方法 |
| NEEDS_HUMAN | 规则或证据不足 | 停止并交人工 |

可续算时使用经过检查的 CONTCAR，INCAR 冻结，最多 3 轮。系统不会为了“跑完”而无限重试或静默修改方法。

完整分类见 [docs/failure-taxonomy.md](docs/failure-taxonomy.md)。

### 5. 分析、任务结果与治理建议

Analyze 依据项目和证据开放相应工具，包括：

- 吸附能与候选构型；
- ΔG 台阶、PDS、限制电位和反应网络；
- DOS/PDOS、d 带中心；
- Bader、差分电荷和功函数；
- NEB、比较与自定义性质；
- 波函数工具及 Multiwfn 当前注册的 14 个分析入口。

缺少结构、参考态、方法一致性、完整页脚、单位或收敛证据时，相应数值保持阻止或 `unknown`。

进入 Analyze 后先看 **capability cards**。每张卡由服务器给出 `available`、`missing_prerequisite`、`mode_mismatch`、`not_implemented` 或 `unavailable`，不可用卡不会执行分析，并只提供一个与当前缺项匹配的后续动作。结果表中的数值、显示精度、parser 模块/版本、来源 opaque ID、manifest/file hash 和分母都由服务器定稿；浏览器不复算科学数值。

当前真实解析边界：

- Electronic structure 可从登记项目成员或 manifest-linked descendant 的 `vasprun.xml` / `EIGENVAL` / `LOCPOT` / `OUTCAR` 解析 DOS/PDOS、能带/带隙与功函数；
- Charge and wavefunction 可从 `ACF.dat` 和 `CHGDIFF.vasp` 解析 Bader 与差分电荷；
- **ELF 分布摘要已接入只读定量解析器**。服务器严格校验 `ELFCAR` 主网格、有限数与 ELF 物理范围，并给出网格形状、原子数、min/max/mean/std、确定性分位点和直方图；这仍不等于成键、盆、临界点或拓扑分析；
- **Task Results** 汇总服务器可解析的登记任务及其证据；不在项目成员/manifest 后代链中的邻近目录不会被偷偷纳入；
- **Property calculators** 目前按 manifest 绑定的 operands 计算表面能、形成/结合能和 VASPsol 溶剂化能；来源不完整、parser 身份/版本缺失或计算失败时 fail-closed。

分析页的 **Next-calculation recommendations** 是治理后的只读建议。它只在当前分析 data fingerprint 绑定到 `human_scientific_reviewed`、允许 final 的 ValidationResult 后，才根据缺失证据、近简并或敏感性信号给出建议。点击确认只生成带 hash 的非可执行 draft intent；其中没有 shell、`sbatch`、`qsub` 或提交 bridge，绝不会自动创建或提交作业。缺少人工复核、final gate 或指纹绑定时建议保持 blocked/unavailable。

## 五、引擎边界

| 引擎 | 当前定位 |
|---|---|
| VASP | 主工作流；完整输入、集群、诊断、派生、分析和报告链 |
| CP2K | 有边界的文件级适配；只支持能力清单内任务和原生单位 |
| Gaussian | 以孤立分子/团簇为主的文件级适配 |
| CASTEP | 有边界的文件级适配；使用 CASTEP 自身输入、赝势与解析合同 |

平台不承诺跨引擎等价。以下行为会被明确阻止或标注：

- 把 VASP ENCUT 直接解释为 CP2K GTH density-grid cutoff；
- 把 VASP PAW 与 Gaussian 基组绝对能量直接比较；
- 在 VASP 与 CASTEP 赝势、截断或参考态未对齐时比较绝对能量；
- 将一个引擎的失败/未收敛 raw energy 当作可信结果；
- 缺少引擎原生单位或方法指纹时自动换算。

## 六、能量与自由能口径

- `E_ads = E(slab+ads) − E(slab) − E(reference)`。
- `E0` 是 DFT 电子能，不自动等同于实验温度下自由能。
- ZPE、热校正和熵项必须显式存在、通过适用性检查并写入证据后才能进入 ΔG。
- 没有频率或热化学证据时，报告必须说明仍是 E0 或不完整校正口径，不能补猜数值。
- 自由能台阶、PDS、`U_L` 和候选评价必须注明参考态、电子数和校正口径。
- 不同项目比较前必须对齐物种顺序、方法指纹、参考态和自由能修正。

真实文献复现 MAE 和投稿状态目前为 `UNKNOWN`。合成演示和测试通过只证明对应软件合同，不证明真实体系计算正确或达到投稿标准。

## 七、报告与发布

当前发布链：

```text
ReportSpec → ReportSnapshot → ValidationResult → revision + manifest
```

ReportSpec 固定用户配置；Snapshot 冻结本次输入和证据；ValidationResult 记录检查、警告、阻断和 claim ceiling；revision 与 manifest 记录最终文件、sidecar、hash 和大小。

报告状态有两条独立轴：

- **产物轴**：格式是否可用、文件是否生成、hash/size 是否一致、manifest 是否登记；
- **科学轴**：当前证据资格、报告用途、claim ceiling 和 publication gate。

生成出一个文件不等于科学上可发布。渲染期间项目变化时，文件可以保留用于恢复，但不会写成当前 ready revision。

Publish → Versions 的报告洞察工具包括：

- **Scientific diff**：必须选择两个权威 history revision；服务端逐个重新校验完整 bundle，再比较 ReportSpec scope、快照/输入 hash、validation checks/status、科学资格、模型和表格数值、图表及 manifest/file hash。日期变化本身不是科学差异；
- **Evidence/Claim Graph**：只从冻结 model/spec/snapshot/validation/claim 记录生成 conclusion、table、figure、check、source、job、file-hash 节点与边。缺失关系明确标记，不从可变 live 项目补猜，也不向浏览器返回本地路径；
- **SI capsule**：先使用目录选择器取得单次、不透明的服务端 token，再生成冻结 preview receipt；只有显式确认后才导出确定性 ZIP。内容包括规范化输入 manifest、合同、模型/图表元数据、Methods、BibTeX、环境/版本、validation records、Research Notebook ledger/限制、capsule manifest 与 `SHA256SUMS`；秘密、绝对路径、附件正文、缓存和 mutable live files 被排除，同名 ZIP 绝不覆盖；
- **DOI-ready 本地 archive**：在 Publish → Export 选择一个 current revision，先运行 dry-run。清单逐项给出归档名、逻辑角色、大小、SHA-256、license/attribution、敏感风险、决定和排除理由，并显示 submission readiness 与 gaps。显式确认后，服务端重新校验同一 revision、manifest、plan、目标目录实体与 confirmation envelope，再使用并发锁、同目录临时文件、fsync、自校验和原子不可覆盖发布。ZIP 包含冻结 spec/snapshot/validation/model、实际报告与权威图表、输入 manifest、Methods、环境/parser version、provenance graph、README/CITATION、archive manifest 和 `SHA256SUMS`。原始 POTCAR、密钥、绝对路径、缓存、临时/可变 live 文件和许可不完整的资源保持排除。

这些工具都只接受当前项目的 opaque ID 和 revision ID。revision stale、bundle 被篡改、项目不匹配或证据缺失时显示 stale/blocked/unavailable。归档生成成功只证明本地 ZIP、manifest 和校验和成立；若 revision 是 diagnostic/blocked，它仍然不是科学验证或 final publication。VASP Catalyst Studio 不会在此流程中上传 archive、申请 DOI 或声称已经发布。

### 格式与可访问性

| 格式 | 当前边界 |
|---|---|
| HTML | 语义结构、语言和图片描述基础；conditional，仍需浏览器/屏幕阅读器和人工复核 |
| DOCX | 文档语言、Heading/Caption、重复表头、图片描述基础；conditional，仍需 Word Accessibility Checker 和人工复核 |
| PDF | ReportLab 可视、可搜索输出；无结构树和图片替代文本，`tagged=false`、`pdf_ua=false`，状态为 partial |

不能把当前 PDF 描述为 PDF/UA，也不能用自动化测试代替无障碍或科学人工审查。详细说明见 [docs/report-state-contract.md](docs/report-state-contract.md) 与 [docs/research-notebook-contract.md](docs/research-notebook-contract.md)。

## 八、网络、依赖与 AI

### 确定性核心

本地生成、解析、门禁、hash、报告模型和大多数分析均不调用 LLM。SSH 只连接用户明确配置的集群。

### External LLM

默认关闭。只有在 Settings 中明确允许项目数据外发并配置凭据后，外部 LLM 才可用于：

- 结果解读；
- 论文方法段/规格表抽取；
- 对话辅助。

LLM 不能修改科学状态、写入 accepted、绕过 ValidationResult、提交或取消作业。“停止回复”只停止当前 AI 响应。详细边界见 [docs/matclaw-integration.md](docs/matclaw-integration.md)。

### DECIMER

DECIMER 默认不包含在 Windows 单文件 EXE 中。用户自行安装后，本地缓存不存在时首次模型准备可能联网；因此不能把图片识别描述为无条件离线功能。RDKit 只影响分子编辑/3D 建模，DECIMER 只影响图片识别，不使用对应能力时无需安装。

## 九、常见问题

| 现象 | 处理 |
|---|---|
| Web 界面无法启动 | 安装 pywebview 和 Edge WebView2；检查 `startup-error.log` |
| `vcs-gui` 只有四页 | 正常，它仍是 legacy Tk；用 `vcs gui` 打开当前 Web 工作台 |
| 看不到某个任务或路由 | 检查当前项目、工作模式和引擎能力，不要通过切模式改写已有作业 |
| 集群首次连接被阻止 | 需要服务器管理员提供可逐字核对的 SHA256 主机指纹 |
| 不能续算 | 只有明确可恢复分类、有效 CONTCAR 和轮次预算满足时才开放 |
| 报告只生成部分格式 | 查看格式 availability 与 accessibility 两条能力记录；部分成功不等于整次发布成功 |
| PDF 不是 PDF/UA | 当前 ReportLab PDF 明确未标记；改用 HTML/DOCX 并做人工无障碍复核 |
| AI 不可用 | 默认关闭；需显式允许外发并配置 key。核心工作流不依赖 AI |
| DECIMER 首次使用联网 | 默认 EXE 不包含 DECIMER；自行安装且无缓存模型时可能需要下载模型 |
| 分析显示 unknown/blocked | 缺少证据时的预期行为；按界面列出的具体缺项补齐 |
| ELF 卡不可用 | 检查作业是否 DONE、ELFCAR 是否完整、方法证据是否可验证；解析器只给 ELF distribution summary，不作成键或拓扑结论 |
| 下一步计算建议不可用 | 需要与当前 data fingerprint 绑定、经人工科学复核且允许 final 的 ValidationResult；系统不会绕过该门禁 |
| Clone/Move 后提示 partial rollback | 不要手工重复执行；保留恢复证据，检查原位置、目标位置、注册表和台账后再决定恢复动作 |

## 十、测试、历史与引用

运行：

```powershell
python -m pytest
```

2026-08-13 已实际完成本地最终门禁：免缓存完整 `python -m pytest` 得到 **3658 passed、5 skipped**，耗时 330.80 秒，并有 **15 条来自 ASE/NumPy 的上游弃用警告**；全仓 Ruff、actionlint 与 23 个第一方 JavaScript 文件的 `node --check` 均通过。PyInstaller `--full` 单文件构建成功，最终 EXE 为 **113,586,017 bytes（108.32 MiB）**，SHA-256 为 `bcadc9046685c62cf1a9157d0ceba49b131190184dbe30073ce4189ec6817e2d`。

2026-08-15 的严格科学指纹/复用变更在隔离 worktree 重新运行免缓存完整 pytest，得到
**3693 passed、5 skipped**（226.23 秒，15 条同类上游弃用警告）；全仓 Ruff、25 个第一方
JavaScript 文件的 `node --check`、变更 Python 文件的 `py_compile` 和 `git diff --check`
通过。本轮没有重建 EXE 或重跑 actionlint，也没有执行真实远程集群/VASP 作业；上段 EXE
哈希仍只证明 2026-08-13 的历史冻结产物。

该最终冻结 EXE 内运行的 `full` 与 `journey` healthcheck 均以退出码 0 完成，且各自返回 `ok=true`、`frozen=true`。`journey` 完成 10/10 个阶段，网络尝试为 0、集群操作为 0，服务重建后的重启持久化检查通过。新增 Project lifecycle、Jobs 状态机、Analysis source/capability、报告洞察、Resume Center、资源预测、实验室策略与下一步建议也保留聚焦 Python 和/或真实 Node 生产脚本回归；冻结 journey 保持 `blocked`/`diagnostic` 科学状态的诚实边界。

这些是本地软件门禁，不能替代 GitHub-hosted 远端 CI；推送后的具体提交仍必须通过远端矩阵。`v4.0.0` 标签尚未创建，CHANGELOG 仍为 Unreleased；5 个 skip 不等于对应功能通过。也没有执行真实远程集群作业或真实科学/实验验证，不能从自动化检查、离线 journey 或 EXE 产物推断科学有效性或发布就绪。

CI 覆盖 Ubuntu/Windows × Python 3.10、3.11、3.12。Windows package-smoke 被配置为构建真实单文件 EXE，并在该最终二进制内部依次运行 `full` 与 `journey` healthcheck；journey 使用隔离 HOME/config、真实 Api/ReportService、最小离线作业、真实 Analysis preview 和 diagnostic HTML 报告，再重建服务检查持久化。

[docs/planning/progress-2026-07-16.md](docs/planning/progress-2026-07-16.md) 是历史快照，不是当前功能或测试基线。逐版本历史见 [CHANGELOG.md](CHANGELOG.md)，历史数字不应机械改成当前数字。

引用见 [CITATION.cff](CITATION.cff)，贡献说明见 [CONTRIBUTING.md](CONTRIBUTING.md)，许可证见 [LICENSE](LICENSE)。
