# VASP Catalyst Studio 使用说明

> 轻量化 DFT 自动化工具。当前版本(**M1 生成区**)负责:**POSCAR + 你自己的 INCAR → 自动生成 VASP 输入四件套**(`INCAR` / `POTCAR` / `KPOINTS` / `POSCAR`)。
>
> 核心原则:**尊重用户 INCAR,绝不强改方法学**。你的 INCAR 一字不改,工具只在“缺项”时追加补全,并给出警告。

---

## 目录

1. [环境要求](#1-环境要求)
2. [安装](#2-安装)
3. [配置(config.yaml)](#3-配置configyaml)
4. [快速开始](#4-快速开始)
5. [命令详解:`vcs gen`](#5-命令详解vcs-gen)
6. [生成的四件套说明](#6-生成的四件套说明)
7. [INCAR 校验与补全规则](#7-incar-校验与补全规则)
8. [KPOINTS 自动推荐](#8-kpoints-自动推荐)
9. [支持的元素(POTCAR 变体表)](#9-支持的元素potcar-变体表)
10. [常见问题与报错排查](#10-常见问题与报错排查)
11. [results/ 目录约定](#11-results-目录约定)
12. [后续路线图](#12-后续路线图)

---

## 1. 环境要求

- Python ≥ 3.9
- 依赖:`PyYAML`、`numpy`(安装时自动装)
- 一份本地 **PAW_PBE 赝势库**(VASP 官方 potpaw_PBE,含各元素变体子目录,每个子目录内有 `POTCAR` 文件)

---

## 2. 安装

在项目根目录(含 `pyproject.toml` 的目录)执行:

```bash
pip install -e .
```

安装后获得命令行工具 `vcs`。验证:

```bash
vcs gen -h
```

> `-e`(editable/开发模式)让你改动源码后无需重装即可生效。

---

## 3. 配置(config.yaml)

工具需要知道你的**本地赝势库在哪**。这由 `config.yaml` 的 `potcar_lib_root` 指定:

```yaml
# 本地 PAW_PBE 赝势库根目录:其下应有 <变体>/POTCAR,例如 Fe/POTCAR、Ta_pv/POTCAR
potcar_lib_root: "E:/V2.0.0/results/inputs/potpaw54/potpaw54/potpaw54/potpaw_PBE/paw_pbe"

llm: {}   # M4 分析区预留(OpenAI 兼容:api_key/api_url/model)
ssh: {}   # M2 集群区预留(跳板密钥等)
```

> ⚠️ **首次使用请把 `potcar_lib_root` 改成你自己机器上的赝势库路径。** 路径用正斜杠 `/`,Windows 也可(如 `D:/potpaw_PBE`)。

### 配置文件的定位优先级

`vcs` 按以下顺序查找配置,先命中者生效:

1. 环境变量 `VCSTUDIO_CONFIG` 指向的文件(显式覆盖,最高优先)
2. **当前工作目录**下的 `config.yaml`
3. 包根旁(项目根目录)的 `config.yaml`
4. 都找不到 → 用内置默认值兜底

三种覆盖赝势库路径的方式,从临时到长期:

| 方式 | 作用范围 | 示例 |
|---|---|---|
| CLI `--lib-root` | 仅本次命令 | `vcs gen ... --lib-root D:/potpaw_PBE` |
| 环境变量 `VCSTUDIO_CONFIG` | 当前 shell 会话 | `export VCSTUDIO_CONFIG=/path/to/config.yaml` |
| 改 `config.yaml` | 永久默认 | 编辑 `potcar_lib_root` |

---

## 4. 快速开始

准备好一个 `POSCAR`(**VASP5 格式**,第 6 行是元素符号)和你的 `my.incar`,然后:

```bash
vcs gen --poscar POSCAR --incar my.incar --calc-type slab -o results/job1/
```

生成的 `results/job1/` 目录含:

```
results/job1/
├── INCAR      # 你的原文 + (若有缺项)自动补全区
├── POTCAR     # 按 POSCAR 物种顺序本地拼接,已过 ENMAX≤ENCUT 校验
├── KPOINTS    # 自动推荐的 Gamma 网格(或你 --kpoints 指定的)
└── POSCAR     # 原样拷入
```

终端会打印生成结果与警告,例如:

```
警告: INCAR 未指定 ENCUT,已按 1.3×max(ENMAX)=400.0 补为 550 eV;如需自定义请在 INCAR 显式给出。
已生成: results/job1/
元素: ['Fe', 'C', 'N']  KPOINTS: [5, 5, 1]
```

---

## 5. 命令详解:`vcs gen`

```
vcs gen --poscar <POSCAR> --incar <INCAR> -o <输出目录> [选项]
```

| 参数 | 必填 | 默认 | 说明 |
|---|:---:|---|---|
| `--poscar` | ✅ | — | POSCAR 文件路径(须 VASP5 格式) |
| `--incar` | ✅ | — | 你的 INCAR 文件路径 |
| `-o`, `--out` | ✅ | — | 输出作业目录(不存在会自动创建;已存在则幂等覆盖) |
| `--calc-type` | | `slab` | 计算类型,决定 KPOINTS 策略:`molecule` / `slab` / `bulk` |
| `--kpoints` | | 自动推荐 | 显式指定 K 网格,如 `--kpoints "5 5 1"`(三个整数) |
| `--no-validate` | | 关闭 | 加上此项则**只照抄 INCAR 原文,不做任何校验补全** |
| `--lib-root` | | 读 config | 临时覆盖本次使用的赝势库根 |

### `--calc-type` 三种类型

| 类型 | 适用 | KPOINTS 策略 |
|---|---|---|
| `molecule` | 孤立分子/气相 | 固定 `1 1 1`(仅 Gamma 点) |
| `slab` | 表面/催化剂平板(默认) | 面内按尺寸推荐,**法向 kz = 1** |
| `bulk` | 三维晶体 | 三个方向都按尺寸推荐 |

---

## 6. 生成的四件套说明

### INCAR —— 原文透传 + 追加补全
- **你的 INCAR 一字不改**,原样写入。
- 若开启校验(默认),缺失的关键项会**追加在文末**一个补全区,带醒目分隔行:
  ```
  # --- vcstudio 自动补全 (validate_and_complete_incar) ---
  ENCUT = 550
  MAGMOM = 12*5 8*0
  ISPIN = 2
  ```
- 用 `--no-validate` 可完全关闭补全(严格照抄)。
- 详见 [第 7 节](#7-incar-校验与补全规则)。

### POTCAR —— 本地拼接 + 硬校验
- 按 **POSCAR 第 6 行的物种顺序**依次拼接各元素的赝势(保证与 POSCAR/MAGMOM 顺序一致)。
- 每个元素自动选用经核对的 PAW_PBE 变体(如 `V → V_sv`、`Ta → Ta_pv`,见 [第 9 节](#9-支持的元素potcar-变体表))。
- **ENMAX ≤ ENCUT 硬校验**:任一元素赝势的 ENMAX 超过 ENCUT 会直接报错、绝不静默生成低精度输入。

### KPOINTS —— 自动推荐或手动指定
- 缺省按晶胞尺寸自动推荐 Gamma-centered 网格(见 [第 8 节](#8-kpoints-自动推荐))。
- 用 `--kpoints "kx ky kz"` 可完全接管。

### POSCAR —— 原样拷入
- 直接复制你的 POSCAR 到输出目录,不改动。

---

## 7. INCAR 校验与补全规则

开启校验时(默认),工具**只对“缺失键”产出补全项**;对“值不合理”只警告、不修改。**从不改动你已经写的任何键。**

| 情况 | 工具行为 |
|---|---|
| **未写 `ENCUT`** | 按 `1.3 × max(ENMAX)` 向上取整到 50 的倍数补一个 ENCUT,并写入 INCAR + 用于 POTCAR 校验 |
| **写了 `ENCUT` 但偏低**(< 元素最大 ENMAX) | 保留你的值,只**警告**;POTCAR 拼接阶段会因 ENMAX>ENCUT 报错,请自行提高 |
| **体系含磁性元素**(V/Cr/Mn/Fe/Co/Ni)**且未写 `MAGMOM`** | 补 `MAGMOM`(每磁性原子 5μB,其余 0,按 POSCAR 顺序)并设 `ISPIN=2` |
| **含磁性元素但你显式设了 `ISPIN=1`** | **尊重你的设置**,不补 MAGMOM,只警告“磁性态可能丢失” |
| **含磁性元素、无 MAGMOM、但 POSCAR 无有效计数** | 无法生成 MAGMOM,退而补 `ISPIN=2` 保证自旋极化,并警告 |
| **你已写 `MAGMOM`** | 完全不动 |

> 说明:补全不含任何“RPBE 时代”的强制偏置(如 ENCUT 400 下限、方法学锁定)。这正是“轻量版”与旧统一版本的本质区别。

---

## 8. KPOINTS 自动推荐

算法(源自过渡金属体系经验值):

- 每个方向:`k_i = ceil(1 / (spacing × |a_i|))`,其中 `spacing ≈ 0.03 Å⁻¹`
- 偶数化奇数(Gamma-centered 惯例):偶数结果 +1
- 上限封顶 9(更高需自己做收敛测试)
- `--calc-type slab`:法向 `kz` 强制为 1
- `--calc-type molecule`:直接 `1 1 1`

输出为 Gamma-centered 自动网格,例如:

```
Automatic
0
Gamma
5 5 1
0 0 0
```

---

## 9. 支持的元素(POTCAR 变体表)

工具内置了经 ENMAX 核对的 PAW_PBE 变体映射,当前覆盖:

**非金属/主族**:C, N, B, P, S, Li

**过渡金属**:Sc, Ti, V, Cr, Mn, Fe, Co, Ni, Cu, Zn, Y, Zr, Nb, Mo, Tc, Ru, Rh, Pd, Ag, Cd, Hf, Ta, W, Re, Os, Ir, Pt, Au

**默认按磁性处理的元素**(缺 MAGMOM 时补 5μB):V, Cr, Mn, Fe, Co, Ni

> 若 POSCAR 里出现**未登记的元素**,工具会报错(而非乱选变体)。新增金属前请先核对其 ENMAX ≤ ENCUT,再登记到 `vcstudio/generate/potcar.py` 的 `POTCAR_VARIANT` 表。

---

## 10. 常见问题与报错排查

| 报错/现象 | 原因 | 解决 |
|---|---|---|
| `POSCAR 缺元素符号行(VASP4 或畸形)` | POSCAR 第 6 行是数字而非元素符号 | 改成 **VASP5 格式**,第 6 行补元素符号 |
| `元素 'Xx' 未在 POTCAR_VARIANT 登记` | 该元素还没进变体表 | 核对 ENMAX 后在 `potcar.py` 登记;或换已支持元素 |
| `Xx(变体) ENMAX=… 超过 ENCUT=…` | 你的 ENCUT 太低 | 提高 INCAR 的 ENCUT,或换低价电子变体 |
| `POTCAR 不存在: …/变体/POTCAR` | 赝势库路径不对或库不全 | 检查 `potcar_lib_root`;确认库里有该变体子目录 |
| `INCAR 的 ENCUT 非数字` | INCAR 里 ENCUT 写了非数值 | 改成数值,如 `ENCUT = 500` |
| `POSCAR 负缩放因子…暂不支持` | 第 2 行缩放因子为负(=目标体积) | 提供正缩放因子的 POSCAR |
| 中文警告在 Windows 控制台乱码 | 控制台编码 | 工具已尝试切 UTF-8;仍乱码可 `chcp 65001` |

> 所有校验错误都**显式报错、绝不静默产出错误输入**——这是本工具的设计底线。

---

## 11. results/ 目录约定

项目自带一个 `results/` 目录作为**生成作业的建议存放处**:

```bash
vcs gen --poscar POSCAR --incar my.incar -o results/job1/
```

`results/` 里的生成内容默认**不纳入 git**(见 `.gitignore`),仅保留目录本身(`.gitkeep` 占位)。你也可以用任意 `-o` 路径,不强制放这里。

---

## 12. 后续路线图

| 区 | 状态 | 职责 |
|---|---|---|
| generate 生成区 | **M1 已完成** | POSCAR + 用户 INCAR → 输入四件套 |
| cluster 集群区 | M2 | 多调度器(Torque/Slurm/LSF/Shell)提交/查询/回收 |
| monitor 监控区 | M3 | 纯 Python 零 token 轮询:收敛/失败检测 → 契约文件 |
| analyze 分析区 | M4 | 吸附能图/台阶/火山 + Word 报告 + 可选 LLM 讨论 |
