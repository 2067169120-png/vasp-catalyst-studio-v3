# 工作日志(滚动,最新在上)

> 给下次会话的接续入口:先读本文件 + `继续开发-交接总结.md`(§1 三不变式)+ README 架构表。

## 2026-07-07(第 7 轮:全流程对照验收 + 图表出版级修缮)

**4 提交**(TITEL闸 `8291324`、图表升级 `00718a8`、提示词语境 `cae7626` 等),**230 测试全绿**。三路对照审计(全流程/图表样式/报告组织)结论:

**对等性判定**:任务检测=桌面更稳(哨兵/终态原因/取证)但缺**运行中作业活体检测**;POTCAR=闸已移植且比原版覆盖更全;提交脚本=对等(确认原版无脚本内续算循环);修复=有界人工触发是刻意差异。**本轮修掉**:TITEL 闸、图表样式(NPG逐物种色/数值标注/红PDS/网格,gl.lt_exec 层作用域是关键修复)、催化剂类型机理语境注入提示词、__init__ 版本刷新。

**遗留清单(诚实,下轮候选)**:①运行中作业 SCF 震荡活体预警(检测逻辑已有,只差接到 RUNNING 分支+GUI 提示);②报告参考文献章节(通用钩子);③火山图/热图接线(需多项目聚合);④离线(无API key)数据驱动讨论段;⑤大规模作业 is_settled 记忆化;⑥流水线自动推进(slab→ads 自动建/投,大特性);⑦远端上传后 TITEL 复核(SFTP 截断,小概率)。

## 2026-07-07(第 6 轮:全部遗留后台任务闭环)

**4 提交**(`f7c3f70` SCF震荡、`d66138f` 报告章节+引号、`a98c57e` 方法学顾问+取证升级、及日志),**228 测试全绿**。当初死于限流的后台任务至此**全部完成**:

1. **SCF 震荡检测**(原版 healer 口径:末离子步≥80 且 |dE|>1e-2 双条件)→ `SCF_SLOSHING` 交人工。
2. **报告提取任务剩余交付**:补原版 4 章节(ΔE 统计/计算参数表/ΔE 着色 <-3绿 >0红/方法学约定)。
3. **对抗审查任务剩余交付**:status_cmd/qdel/scancel 防御性引号;审查结论正式归档(Ready)。
4. **网研重跑落地**(wuk87qqv6 → wwxcmn43g,VASP Wiki/custodian/pymatgen/ASE 权威来源):
   - **修真 bug**:VASP5 NELM 耗尽也打印 EDIFF-reached → 静态收敛判据加 `nelm_saturated` 守卫
   - STOPCAR → `USER_STOPPED`(防人工叫停被盲续);干净退出页脚细化证据(跑满 vs 被杀)
   - **方法学顾问 `project/advisor.py`**(9 条 warn-only 规则):ENCUT 全员统一(P0,算出推荐值)、
     气相参考 ISMEAR/SIGMA(P0)、开壳层 ISPIN(P0,O₂ 三重态)、LDIPOL/IDIPOL/DIPOL、
     四面体弛豫、参考盒尺寸、O₂ NUPDOWN;建项目时自动体检,项目页日志展示
   - **未落地留档**(下轮候选):EDIFFG 末步力终检(需 TOTAL-FORCE 表+冻结掩码解析)、
     EENTRO 熵一致性告警、真空层/冻结掩码几何检查(需 POSCAR 坐标分析)、
     custodian 级"连续 10 离子步打满 NELM"计数

## 2026-07-06(第 5 轮:optimization-review-round2 收尾)

**1 提交**(`57c39eb`),**214 测试全绿**。那轮多维审查的 Verify 阶段曾撞额度死掉 31 个验证任务(确认的 6 项当时已修),本轮逐条核销剩余:补 EDDRMM/EDWAV/WAVECAR_CORRUPT/INCAR_READ 四类 Custodian 签名;Slurm oom-kill/MPI Killed 日志文本直接判 OOM;E0=0.0 判不可信;**静态作业收敛判据分流**(static/dos/band 查电子标志 'aborting loop because EDIFF is reached',离子标志判静态会全部误判未收敛——对 roadmap 的 static/dos/band 链是前置正确性修复)。有意不收:Sub-Space-Matrix 单次告警(防误报)、NELM 重复计数(需 OSZICAR SCF 块解析,后续)。**review-round2 至此全部闭环。**

## 2026-07-06(第 4 轮:审计遗留收尾 + 对标调研)

**2 提交**(`cac4303` POTCAR 溯源入 job.yaml、`659c68d` 任务页"重新判定"按钮),**210 测试全绿**。审计遗留项全部关闭。

### 对标调研(5/8 档案已验证,Opus 子代理产出)

| 产品 | 机构/发表 | 定位 | 与我们的关系 |
|---|---|---|---|
| ChemCrow | EPFL+Rochester, *Nat Mach Intell* 2024 | 有机合成 agent(18 工具,ReAct+LangChain,文献 RAG,机器人执行) | 不做 VASP/DFT,不重叠 |
| CACTUS | PNNL, *ACS Omega* 2024 | SMILES 化学信息学 agent(10 个 RDKit 工具,开源 7B 可跑) | 不做 VASP/DFT |
| ChatMOF | KAIST, *Nat Commun* 2024 | MOF 检索/预测/逆设计(MOFTransformer 代理模型+遗传算法) | 运行时无 DFT |
| dZiner | UToronto AC, arXiv 2024 | 逆设计 agent(文献 RAG 设计规则,human-in-the-loop,RDKit 护栏) | 刻意避开 DFT |
| SciToolAgent | 浙大, *Nat Comput Sci* 2025 | 500+ 工具知识图谱编排(Planner-Executor-Summarizer,94% SciToolEval) | 通用工具编排,非 VASP 专用 |

**共性能力(我们缺)**:①自然语言任务入口(5/5 都有);②文献 RAG 接地(ChemCrow/dZiner);③工具依赖图谱式规划(SciToolAgent);④ML 代理模型性质预测(ChatMOF/dZiner);⑤标准化评测基准(SciToolEval 式——对论文很有用:可做"失败分类器在录制日志集上的准确率"评测)。
**我们的差异化(论文卖点,5 家都没有)**:真实 HPC 接地(PBS/Slurm+跳板)、零 token 确定性核心(OpenClaw 一次 $7.6,我们 ~$0 且可复现)、生产级失败取证分类学+有界恢复、方法学主权、单文件 EXE 面向实验课题组、全链溯源(sha256+POTCAR TITEL)。

**后 3 份已到(8/8 齐,全部一手来源验证)**:

| 产品 | 发表 | 定位 | 关键事实 |
|---|---|---|---|
| **El Agente Q** | Aspuru-Guzik 组, *Matter* 2025(arXiv 2505.02484) | 分层多代理量化工作流(ORCA),>87% 任务成功率 | 架构最强的竞品,但 **只做分子/ORCA 不做 VASP,且未开源**(elagente.ca 预注册制);自曝上下文压力(单任务 320 万 token)与关键词幻觉 |
| **LLaMP** | Berkeley/LBNL, arXiv 2401.17244 | Materials Project 分层 RAG 检索 | **不跑 VASP**,只检索 MP 预算数据;atomate2 仅是可选挂件 |
| **VASPilot** | 中科院物理所, *Chin Phys B* 2025(arXiv 2508.07035, LGPL) | **唯一直接竞品**:CrewAI+MCP 多代理 VASP 全流程(MP 取结构→生成输入→Slurm→错误解析→重投→验证→绘图,Web UI,DeepSeek-V3) | 弱点=我们的卖点:**参数决策全由 LLM 生成、无审计、无系统性可靠性研究**;人工监督仍不可缺 |

**竞争定位一句话(论文用)**:8 个同类系统里只有 VASPilot 做 VASP,而它把 INCAR 交给 LLM;我们是唯一"确定性核心保证可复现物理正确输入 + LLM 只在分析边缘"的 VASP 系统,且有生产级失败取证分类学、有界恢复与全链溯源——这正是 OpenClaw 论文承认"仍需人工把关"的科学有效性层。

### Top-5 下轮开发建议(按发刊+科研价值排序)
1. **评测基准(发刊必需)**:仿 SciToolEval——失败分类器在录制日志集上的准确率 + 端到端任务套件。竞品都有数字(El Agente 87%/SciToolAgent 94%),评审必问。素材现成(tests fixtures + 真集群日志)。
2. **收敛性测试自动化**(ENCUT/k-mesh 系列作业一键生成→批量提交→E-参数收敛判定):VASPilot 演示过;吸附能可信度刚需;charts.py 已有 convergence 图类型,纯确定性实现零新依赖。
3. **自然语言任务入口(可选)**:LLM 只把"我要算 Pt(111) 上 CO 吸附"翻译成**受 schema 验证的项目规格**,执行仍走确定性核心——对齐论文"任务清单由 LLM 生成"卖点,零 token 核心不破。
4. **Materials Project 结构检索**(零依赖 REST):LLaMP/VASPilot 都有;免去用户手备 POSCAR,urllib 即可,需用户申请免费 API key。
5. **吸附位点枚举**(top/bridge/hollow 自动摆放):做成 external/ 可选依赖适配器(pymatgen 存在则用),缺依赖时降级手工模式。

## 2026-07-06(第 3 轮:发刊级结构 + opus4.8 报告 LLM + GUI 主题)

**分支** `feature/gui-exe-config`,本轮 4 提交(`77665d5`…`889ff40`),**209 测试全绿**。

### 本轮完成
1. **报告 LLM 切至用户网关**(`77665d5`):端点 `http://23.144.4.56:8317/v1/chat/completions`,模型 `claude-opus-4-8`,key 在 Windows 凭据库(service=`vcstudio-llm`)。**真调全通**(zh 385 字+en 972 字+5 caveats)。踩平并代码化 4 个网关坑:transport 必须返回 (status,body) 而非抛 HTTPError;400 拒 `temperature` 自动剥参重试;Claude 的 ```json 围栏用 `_extract_json` 切片;键名约束放 user 提示词末尾(recency)。⚠ 注意:**根目录 config.yaml 优先于 %APPDATA%**,两处 llm 都已写。网关通道间歇 401/502,重试预算 4 次。
2. **发刊级审查修复**(`51e7c03`,上一轮遗留):接通调度器终态原因(`parse_terminal`+`query_scheduler`),超墙钟 137 不再误判 OOM;BAD_ENERGY 存 `raw_energy_e0_eV` 防漏入 ΔG;freeenergy 加 state==DONE 门;被卡项目日志点名。
3. **图表配色**(`4adb824`):全线 Paul Tol bright(#4477AA…),charts.py + origin runner 两处,真机渲染目检 ✓。
4. **GUI 主题**(`889ff40`):`gui/style.py` 统一视觉(clam+Tol 主色+Segoe UI),三个主按钮 Accent 化,LogBox 白面等宽。
5. **文档**:README(论文分层架构表+科学约定)、使用说明(分区块代码地图)重写;`docs/2026-07-05-夜间优化完成报告.md` 留档。
6. **用户配置全就绪**:potcar 库/分子库/理想窗口/llm 端点+模型/key,**EXE 重打包后即可直接测试**。

### 进行中(下次会话接手)
- **对标调研**(Opus 子代理 `a437b15e0c6af1416`,后台):ChemCrow/CACTUS/VASPilot/El Agente/ChatMOF/LLaMP 等产品对标 → 特性缺口排序。结果文件:`…\tasks\a437b15e0c6af1416.output`(若已完成直接读 result 字段)。
- 调研结论落地:挑高价值缺口(预期:吸附位点枚举、收敛性测试自动化、NL 任务规格、文献 RAG)排下轮开发。

### 论文距离评估(对照 OpenClaw, JCTC 2026)
| 论文要素 | 我们 | 距离 |
|---|---|---|
| 显式任务清单+验证条件 | job.yaml 状态机+preflight+终态取证 | ✅ 已对齐(静态清单;论文是 LLM 动态生成) |
| 有界恢复 | 分类→建议→3 轮上限→NEEDS_HUMAN 停机 | ✅ 已对齐且更保守(方法学主权) |
| HPC 接地 | 自研 PBS/Slurm 方言层(非 DPDispatcher) | ✅ 等价实现,LSF 未做 |
| 技能生态/可替换模块 | 分区块模块(适配器化 Origin/POV-Ray) | ✅ 结构对齐;无运行时技能加载(桌面 EXE 定位不同) |
| LLM 编排 | 刻意不用:确定性核心零 token,LLM 只在分析层 | ⚖ 设计差异点(论文卖点是 agent 编排,我们卖点是可复现+零成本) |
| 最终研究摘要 | HTML 完整报告+AI 双语分析 | ✅ 已对齐 |
| 缺口 | 自然语言→任务规格、POTCAR 溯源入 manifest、NEEDS_HUMAN 确认按钮、集成测试阶梯 | 下轮补 |

### 已知遗留(不阻塞)
- POTCAR 溯源(variant/TITEL/ENMAX)写入 job.yaml;NEEDS_HUMAN 确认/重置操作;LSF 方言;`test_analyze_http_error_retries_then_fails` 因重试 sleep 变慢(4.5s,可注入 sleep 优化)。
- 网关偶发 401/502 是通道问题,报告层已降级处理;若持续可让用户换通道。

## 2026-07-05(第 2 轮:失败验证+报告+有界恢复;M4 视觉/AI 层)
详见 `docs/2026-07-05-夜间优化完成报告.md` 与 git log `4ffa4b6..6d672a9`。要点:diagnose.py 12+11 类分类器(Custodian 核对)、CONTCAR 续算、零依赖 HTML 报告、Origin/POV-Ray/自由能/AI 分析全链、S5 自动轮询、全 DONE 自动出报告。

## 2026-07-02(第 1 轮:S1-S4)
见 `继续开发-交接总结.md`(用户保管的交接文档,git 未跟踪)。
