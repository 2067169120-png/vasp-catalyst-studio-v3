# 夜间优化完成报告（对齐论文 · 失败验证 + 报告 + 有界恢复）

日期：2026-07-05（通宵自主）　分支：`feature/gui-exe-config`（现领先 `master` 33 提交）　测试：**146 全绿**

你睡前把三件事交给我:从原版本 `E:\V2.0.0\` 学、对齐参考论文、大量网搜 VASP 吸附能知识,来补强**任务失败验证**与**报告生成**。下面是我做完的东西、依据、以及你醒来要做的。

---

## 一句话结论

原来"作业没成功"只有一个 grep、全部压成 `UNCONVERGED`,而 `FAILED`/`NEEDS_HUMAN` 两个状态**声明了却从没代码置入**(死态)。现在有了一套**纯函数失败分类器**(12 类,签名取自原版本真实生产代码)、把它接进状态刷新**复活了死态**、加了**物理合理性闸**(防"收敛却是垃圾数"入表)、一个**零依赖 HTML/Markdown 批量报告**、以及对齐论文的**有界 CONTCAR 续算**。全部 TDD、零新依赖、守住你的三条不变式。

---

## 学到了什么

- **论文**(OpenClaw, *J. Chem. Theory Comput.* 2026):要对齐的三点 = 每阶段**显式验证条件** + **有界恢复**(分类→有界修正→重试上限→停机/交人工,绝不无界) + **最终研究摘要**。论文明说科学有效性(收敛/采样)仍需人工把关——正是要硬化的地方。
- **原版本**:一个并行研究 workflow 逐行审计了它的失败检测/恢复/报告,产出 9 条缺口分析。它的失败签名(SIGSEGV / ZBRENT / OOM=退出 137 / 沉默退出 / 缺输出 / NELM 震荡 / 力爆)是**真集群验证过的领域知识**——这正好补上了网搜的缺口。
- **网搜**:很遗憾,网研 workflow 和两个定向抽取 agent **撞上了会话额度限制**(8:30 重置),没跑出来。但如上,原版本已把这些 VASP 失败签名沉淀成代码,加上我自己的 VASP 知识,足以扎实落地——**没被卡住**。醒来后如果你想,我可以在额度恢复后补跑网研,把 pymatgen Custodian 的完整 handler 目录再交叉核对一遍。

---

## 做了什么(6 个提交,逐块 TDD)

| 提交 | 内容 | 对齐点 |
|---|---|---|
| `4ffa4b6` | **`cluster/diagnose.py`** 失败分类器(纯函数):综合 调度器终态+退出码+OUTCAR/OSZICAR 完整性+日志硬崩签名+收敛串+能量合理性 → 12 类 + 目标状态 + 可否续算 + 证据。含 `valid_poscar`、通用能量合理性界。24 测试 | 论文"验证条件";三不变式③显式状态 |
| `a9e000e` | **`refresh_job` 接入分类器**:SSH 取证(stat 大小 / EXIT 标记 / 日志尾)→ classify → **复活 FAILED/NEEDS_HUMAN**;**物理合理性闸**(收敛但 E=None/>0/\|E\|>1e4 → NEEDS_HUMAN);结构化 `results.diagnosis` + 失败 `attempts` 回写 | 论文可溯源;堵住"缺 OUTCAR 误当未收敛"P0 假阴性 |
| `e4901fa` | **`project/report.py`** 批量报告:扫所有 job.yaml → 状态分布 + 失败分类分桶 + 问题作业清单(带证据)+ 能量表 → **自包含单文件 HTML**(内联 CSS 零外链)或 Markdown;可嵌 ΔE。9 测试 | 论文"最终摘要";零依赖守 EXE 体积 |
| `c78c6bf` | **`continue_from_contcar`** 有界恢复:只对可续算分类出手、CONTCAR 过校验、**INCAR 冻结**、`continue_rounds` 上限 3、清 WAVECAR/CHGCAR、prev_job_id 溯源。4 测试 | 论文"有界恢复";三不变式①方法学主权 |
| `a869a53` | **GUI**:任务页加"♻ 续算(选中)"+"📄 导出报告"按钮 | 让新能力可用 |
| (spec) | `docs/superpowers/specs/2026-07-05-failure-verification-and-reporting-design.md` 设计文档 | — |

### 失败验证:前 vs 后

| 场景 | 之前 | 现在 |
|---|---|---|
| 缺 POTCAR / 启动即死(零输出) | `UNCONVERGED` | `NO_OUTPUT` → `NEEDS_HUMAN` |
| 段错误 / OOM(退出 137)/ 节点故障 | `UNCONVERGED` | `SIGSEGV`/`OOM`/`NODE_FAIL` → `FAILED` |
| ZBRENT / 墙钟 / SCF-几何未收敛 | `UNCONVERGED` | 同类但标 **可续算**,一键 CONTCAR 续 |
| 收敛了但能量是正数/爆掉(结构重叠) | `DONE`,垃圾数进 ΔE 表 | `BAD_ENERGY` → `NEEDS_HUMAN`,拦在表外 |

---

## 明确没做（守不变式 / EXE 约束，缺口分析 do_not_port）

自动改写 INCAR 的 HealAgent(原团队自己弃用,毁 E_ads 可比性)、Li-S/SAC 科学层、Origin/POV-Ray 绘图、python-docx 固定方法横幅、SMTP 邮件、桌面 toast、SQLite/并行状态存——都不移植。

## 已知延后（不阻塞，醒来可议）

- **调度器终态原因捕获**(缺口#1):Slurm 要 `sacct` 才看得到 TO/OOM/CA/NF(现用 `squeue` 看不到已结束作业),PBS 的 `C` 态本就歧义 → **取证(OUTCAR/日志)才是权威**,这些情况已被分类为可续算,边际价值低,故延后。
- 自动轮询续算、系统通知、matplotlib 图。
- **网研补跑**:额度恢复后可交叉核对 Custodian handler 全表。

---

## 你醒来要做的(只有真集群能验)

代码侧我能验的都绿了(146 测试 + 4 页 GUI 无头构造)。真集群端到端只能你来跑,建议:任务页配 1w → 提交一小组 → **查询状态**(现在会给出 DONE/FAILED/UNCONVERGED/NEEDS_HUMAN + 诊断而非笼统未收敛)→ 对未收敛的点**♻ 续算** → **📄 导出报告**看 HTML。全通过后即可 `feature/gui-exe-config` → `master` 合并(Task 13 收尾)。

工作区干净;你新加的 `继续开发-交接总结.md` 和 `清理临时文件.bat` 我按约定**没动**(bat 要 GBK+CRLF,交接文档是你的)。
