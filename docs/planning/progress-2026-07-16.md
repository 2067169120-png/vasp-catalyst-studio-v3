# vcstudio 开发进度与待办 · 2026-07-16

> [!IMPORTANT]
> **历史进度快照（2026-07-16，非当前 V4 状态）**
> 本文原样记录当日的设计、进度与待办，不描述当前 V4 的实现或发布状态。正文中的
> 历史版本号和测试数量只适用于当时的代码快照，不应解释为当前版本或当前测试结果。

本文件是跨会话生命线:记录今天做完的、以及按用户视角/发刊目标整理的待办备案。

## 一、今天做完(分支 improve/optimization-2026-07,均测试通过入库)

| 提交 | 内容 | 测试 |
|---|---|---|
| 批1 加固 | config.yaml 脱敏(移除内网 IP)、版本号单一事实来源、依赖声明(gui/packaging/charts/dev)、CI 加 lint+覆盖率+打包冒烟、ruff 配置 | 424→ |
| 批2(一) | **修复续算状态误识别**(续算后仍排队却显示已完成/需续算/sloshing —— 读了旧 OUTCAR;加沉降护栏+5回归测试,PBS/Slurm 均覆盖);calc_type 硬编码修复(加计算类型下拉);KPOINTS 改倒格矢(修六方/hcp 欠采样) | +9 |
| 批2收尾 | 项目内 ENCUT 强制统一(保吸附能 ΔE 不被不同基组污染);诊断补 DISK_FULL/裸137降级为疑墙钟/ZBRENT 扩展签名 | +6 |
| 原生出图引擎 | external/native_charts.py:论文级风格层(serif/矢量PDF/多面板/色盲色板)+吸附能柱状图/数据表/ΔG台阶图,延迟import守EXE体积 | +13 |
| 任务分组+首页 | 任务页按吸附能组归并折叠(解决"结果栏混乱")+ dashboard 首页落地 | +3 |
| 分析增强 | 可选ΔG口径(默认ZPE−TS对齐论文/可切ASE严格式)、PDOS投影+d带中心、Bader电荷、PDS/U_L图数一致性修复 | +21 |
| 出图扩展 | 热图(多催化剂×吸附质)、火山图(自动求Sabatier峰顶)、标度关系(拟合+R²) | +12 |

当前:476+ 测试通过,ruff 全绿。流程图见 docs/vcstudio-flowchart.png。

## 二、关键集成缺口 → ✅ 已完成(同日晚)

**native_charts 出图引擎已接进 GUI + 打包收录**:
- ✅ api.py 新增 proj_figures(单项目:bar/table/ladder)与 proj_compare_figures
  (多项目:heatmap/scaling/volcano),ladder 透传逐电子 pds_index(图数一致);
- ✅ 前端项目页「论文级出图」卡片:勾选图类型 + 多项目勾选列表,生成后自动打开图目录;
- ✅ build_exe.py 默认收录 matplotlib(--collect-data matplotlib 带 mpl-data),
  --no-charts 可关;并条件收集 pythonnet/paramiko/nacl(打包审查 P0);
- ✅ gui_web/__main__.py 启动兜底:失败写 APPDATA/startup-error.log + MessageBox
  (治 --windowed 白屏无法自诊)。
- 尚余:报告(report_full)出图优先级切原生引擎(现仍 Origin→SVG);上传"外部已有
  结果"出图出报告的导入通道。

## 三、图表菜单备案(据高影响论文调研,详见项目文档 图表菜单调研)

按"数据依赖 tier"分级,前端做成可勾选菜单:
- **Tier0 仅需ΔE(默认勾选)**:吸附能柱状图、吸附能矩阵热图、ΔG台阶图(查表快速版)、施加电位ΔG、极限电位/过电位对比、结构俯视/侧视图
- **多组自动解锁**:火山图(N≥3且描述符R²达标)、标度关系图
- **Tier1 加flag/轻量算**:PDOS、d带中心相关性、Bader电荷、精确ΔG(频率IBRION=5/6)
- **Tier2 需多个静态点**:差分电荷密度
- **Tier3 外部工具/架构改动**:COHP(LOBSTER)、NEB能垒、AIMD稳定性

## 四、新手跑通全流程待办(据用户视角PRD,详见项目文档 新手PRD)

第一波(开机即卡三堵墙):
1. 首启配置向导 + **设置页**(赝势库/默认精度/LLM API+key/提示词预设)
2. **INCAR 参数向导 + 精度档位**(粗测/标准/高精 → 可编辑草稿)
3. **结构构建**(slab切割+吸附位点枚举+固定底层+真空;先做固定底层+真空)
4. **计算环境选择**:VASP 版本(5.4/6.x)、可执行名(vasp_std/gam/ncl)、赝势版本(.54/.52)、并行(NCORE/KPAR 自动推荐)

第二波:失败一键改参重投、资源校验(account/mem/qos+集群模板)、赝势变体选择
第三波:参数合理性检查扩展(真空/+U/k点)、可复现导出包+xlsx+图批量导出、结果归档+磁盘清理、完成通知
加分:CIF/Materials Project导入、内置示例+术语tooltip、墙钟预估、通用反应自由能引擎(ORR/HER/CO2RR)

## 五、打包与首启待办(据用户即将打包EXE,详见项目文档 打包PRD)

P0(会打不开):
- gui_web/__main__ 加启动 try/except → 写 APPDATA 日志 + MessageBox(否则 --windowed 下失败=白屏无法自诊)
- 干净 Windows 实测三条运行时链:开窗(WebView2/pythonnet clr)、连集群(paramiko/cryptography)、存密码(keyring);按需补 --collect-all pythonnet / --collect-submodules paramiko
P1:
- native_charts 若发布:去 EXCLUDES + --collect-data matplotlib + 接线(见第二节)
- config.example.yaml + examples/ 打进 EXE;GUI 加"载入石墨烯示例+假赝势库"一键跑通
- 赝势库空态可操作引导;首连集群引导
P2:--onedir或splash改善首启慢;--icon+版本资源;代码签名

## 六、发刊路线(详见项目文档 发刊路线图)
- 定位:桌面端×确定性×催化专用,填 VASPKIT(只后处理)/atomate·AiiDA(需编程)/LLM-agent(不可复现)空白
- 差异化:确定性核心消解了 OpenClaw(JCTC 2026)等 LLM-agent 的软肋(不可复现/silent error/token成本)
- 需补:custodian错误覆盖对照表、功能矩阵对比、code metadata、**复现张洪毅 SAC 体系案例**
