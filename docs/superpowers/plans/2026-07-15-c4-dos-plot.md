# C4 DOS 出图 — 实现计划

> Spec:`docs/superpowers/specs/2026-07-15-c4-dos-plot-design.md`。
> 全局约束同 C1-C3:窄 pathspec、TDD、全量 pytest 全绿(基线 401 passed 1 skipped)、零新依赖。

### Task 1: 解析 `project/dosparse.py`
- iterparse 流式抓 <dos>(efermi + total 双自旋),</dos> 即停;file-like 输入。
- [ ] 失败测试(tests/test_dosparse.py:ISPIN=1/2、无 dos 段、截断 XML)→ 实现 → 全量绿 → `git commit -- vcstudio/project/dosparse.py tests/test_dosparse.py`

### Task 2: 渲染 `project/charts.py::render_dos_svg`
- 出版风格:E−E_F 横轴、自旋镜像、E_F 竖虚线、PUB_COLORS、_nice_ticks 复用。
- [ ] 失败测试(tests/test_charts.py 追加:SVG 关键元素)→ 实现 → 全量绿 → `git commit -- vcstudio/project/charts.py tests/test_charts.py`

### Task 3: api `dos_view(job_dir)` + 前端「DOS」按钮
- 缺 vasprun.xml → error(提示 FETCH_FILES 无它,需手动放置;不改拉回清单);落 job_dir/dos.svg(写失败仅 warnings);注入 dos_mod。前端 modal 内嵌 SVG + 已存路径提示。
- [ ] api 失败测试 → 实现;jobs.js 接线;node --check;真浏览器 harness 验 SVG 渲染后删;全量绿 → `git commit -- vcstudio/gui_web/api.py tests/test_web_api.py vcstudio/gui_web/assets/dos.js vcstudio/gui_web/assets/jobs.js vcstudio/gui_web/assets/index.html`

### Task 4: 终审(自验探针模式)+ 台账
